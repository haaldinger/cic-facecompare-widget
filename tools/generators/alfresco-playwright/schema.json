{
    "$schema": "https://json-schema.org/schema",
    "$id": "AlfrescoPlaywright",
    "title": "",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Name of the library",
            "$default": {
                "$source": "argv",
                "index": 0
            }
        },
        "directory": {
            "type": "string",
            "description": "A directory where the library is placed. The path is relative to the project root."
        },
        "strict": {
            "type": "boolean",
            "description": "Create a library with stricter type checking and build optimization options.",
            "default": true
        },
        "provideExample": {
            "type": "boolean",
            "description": "Define if the boilerplate should contain an example of Page and Component.",
            "default": true
        },
        "appName": {
            "description": "The app name you would like to hit.",
            "type": "string",
            "enum": ["studio-hxp", "workspace-hxp", "admin-hxp", "none"],
            "default": "studio-hxp"
        },
        "linter": {
            "description": "The tool to use for running lint checks.",
            "type": "string",
            "enum": ["eslint", "none"],
            "default": "eslint"
        },
        "skipFormat": {
            "description": "Skip formatting files.",
            "type": "boolean",
            "default": false
        },
        "importPath": {
            "type": "string",
            "description": "The library name used to import it, like `@my-org/my-awesome-lib`. Must be a valid npm name."
        },
        "standaloneConfig": {
            "description": "Split the project configuration into `<projectRoot>/project.json` rather than including it inside `workspace.json`.",
            "type": "boolean",
            "default": true
        },
        "tags": {
            "type": "string",
            "description": "Add tags to the library (used for linting).\nSuggested tags: 'scope', 'type', 'category', 'environment'"
        }
    },
    "required": ["name"]
}
